[Unit]
Description=React App
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/TechtileDashboard/dashboard
ExecStart=/usr/bin/serve -s build -l 3000
Restart=on-failure
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
